package me.recursiveg.autoharvest;

public enum EnumSeedType {
    NORMAL, // normal seeds are placed on top of another block
    COCOA,  // cocoa needs to be placed on sides of jungle log
    INVALID
}
